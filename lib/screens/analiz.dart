import 'package:flutter/material.dart';

class Analiz extends StatelessWidget {
  const Analiz({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("analiz sayfası")));
  }
}
