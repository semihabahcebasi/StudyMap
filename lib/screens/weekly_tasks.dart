import 'package:flutter/material.dart';

class WeeklyTasks extends StatelessWidget {
  const WeeklyTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Haftalık Görevler")));
  }
}
